# 如何复刻《溪间四时》

**原作创意、视觉方向与需求设计：AC**  
本指南用于学习与二次开发。发布衍生版本时请保留 AC 的署名与项目许可证。

## 1. 先定义不可变的体验目标

先写下三条约束：场景是手机优先的竖向插画构图；每个物体必须在三维空间里有可信支撑；时间、季节、天气、动物和声音共用一条可暂停的时间线。后续任何细节都不应破坏这三条。

## 2. 建立工程

推荐 React、Three.js 与 TypeScript。页面只需要一个全屏 Canvas 和一层无底板 UI。将场景拆成独立模块，而不是把所有逻辑堆在一个组件里：

- `world.ts` 负责场景装配、渲染循环和统一状态。
- `garden-clock.ts` 负责时辰、日月方向和自动四季。
- `terrain-footprint.ts` 用一组沿河道变化的控制点生成不规则土地、水面和溪底。
- `ancient-tree.ts` 与 `tree-profile.ts` 负责古树曲线、分叉、树冠和上部连续增高。
- `seasonal-life.ts`、`day-night-life.ts` 与 `seasonal-fauna.ts` 分别负责摆件、昼夜替换与动物。
- `pixel-renderer.ts` 负责原生/像素后处理、有限色阶、抖动与墨线。

先完成可旋转的土地、溪流、房屋和一棵树，再加四季。不要在构图尚未稳定时先做大量小物件。

## 3. 地形与构图

使用一条中心曲线和随纵向变化的左右宽度生成溪流。土地边界由稀疏控制点插值，并给顶面、侧面和溪岸分别建几何，避免厚方盒。相机使用正交投影，收集主要几何的包围点后按竖屏、横屏、桌面分别计算取景；相机目标点应允许上下平移。

轮廓后处理读取深度与法线，对几何边界和明显折面勾线。透明粒子、花草细线和水面高光应排除或减弱轮廓，否则画面会变脏。像素模式先把场景渲染到按 CSS 像素缩小的目标，再用最近邻放大。

## 4. 古树

主干和枝条使用 Catmull–Rom 曲线与环形截面生成。半径从根部连续收窄，末端至少增加两个偏转控制点并缩到细枝半径，冬季无叶时也不会出现平截面。根部使用多条贴地曲线形成板根。

树冠不要只在枝端放球状叶团。沿主枝肩部、中段、分叉和末端分层布置实例化叶片，并保留少量天光缝。需要盖住房顶时，先从真实母枝上取切线，生成连续连接的长枝，再沿长枝放叶簇。落叶系统从当季仍可见的叶实例位置采样。

## 5. 房屋、物件与季节

房屋先做地板、柱、梁、障子和瓦片的可读轮廓。每个摆件记录季节与昼夜显隐条件；共用道具只建一次，通过状态切换位置或可见性。对角色和动物使用父子骨架组，让头、颈、身体、四肢拥有明确连接；动作路径必须先检查起点、转弯和落脚面的空间关系。

将四季视为同一庭院的材质与事件状态，而不是四套场景。季节切换时更新叶色、草地、花期、屋瓦落叶、薄冰、积雪和道具显隐。所有动画都接收统一的场景时间，暂停时不推进。

## 6. 时辰、光影与天气

把 0–24 小时映射到太阳和月亮的艺术化轨迹。日光与月光各使用一个方向光；地平线附近先降低旧光源，再抬升新光源。颜色在清晨、正午、傍晚和夜间之间连续插值，同时更新背景、水面月光方向和灯笼强度。

天气状态只允许：春夏秋的晴/雨，冬季的晴/雪。自动天气应使用一个独立计时器，按季节与昼夜给概率，并让手动选择拥有一段保留时间。雨雪的可见度用屏幕像素衡量；积雪应有上限并使用对象池复用滑落雪块。

## 7. 声音与歌单

背景音乐用 `<audio>` 元素与有序歌单循环播放，默认开启；浏览器拦截自动播放时在首次点击或触摸后启动。暂停动画时暂停音乐。程序合成音效已全部移除。

## 8. 响应式 UI

UI 使用绝对定位覆盖场景：左上是印章、眉题和标题；右上是题记、语言与场景工具；底部是季节、画风、昼夜、天气、暂停、声音和时辰。桌面横向排布，手机竖屏分成两行，横屏压缩高度。提供 `100vh` 基线，再用新视口单位增强；安全区同时兼容容器变量与 `env()`。

触摸规则固定为单指旋转、双指平移与缩放。按钮提供可读的 `aria-label`，不要依赖悬停才能显示关键操作。

## 9. 小红书小工具版本

小工具是离线 H5。构建时输出根目录 `index.html`、经典 `assets/app.js`、`assets/style.css` ：

1. 将 JavaScript 转译到 ES2017 / Chrome 61，禁止 module、内联脚本、网络请求、Worker、WASM 与外部资源。
2. 使用相对路径；不要包含 `node_modules`、source map、构建配置或多余外层目录。
3. 隐藏网页全屏按钮。拍照时把 Canvas `data:` URI 传给 `writeTempFile`，再调用 `saveImageToPhotosAlbum`。
4. WebGL 默认使用 1× DPR 与较小阴影图，页面隐藏时不补算时间；初始化失败时显示可理解的错误状态。
5. 小红书专用包不包含音乐文件或播放器，遵守平台文件扩展名白名单。对产物目录和 ZIP 分别运行官方审计脚本，再检查禁用 API、外部链接、入口层级和相对路径。

## 10. 验证顺序

先运行类型检查和生产构建，再运行场景结构测试；随后生成单文件 HTML 和小工具包。小工具包至少检查：`index.html` 在根目录、无外部 URL、无内联脚本、无 module、无全屏与下载 API、资源全部存在、ZIP 小于 10MiB。最后将同一提交发布到公开站点与开源仓库，避免网站、离线包和文档版本不一致。



小红书上传限制更新：专用 ZIP 仅包含 HTML、CSS、JS，移除所有音乐文件、播放器与音量按钮；网页版保留六首音乐。以平台实际文件扩展名白名单为准。
